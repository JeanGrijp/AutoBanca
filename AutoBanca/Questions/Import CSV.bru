meta {
  name: Import CSV
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/questions/import
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  file: @file()
}

settings {
  encodeUrl: true
}
